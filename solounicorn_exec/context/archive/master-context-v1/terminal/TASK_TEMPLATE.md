# Bounded task packet

Copy into the repository's execution task folder. Fill the fields for an actual assignment; this template is not a live task.

- Task ID / parent milestone / owner / status:
- Player-visible or engineering outcome (one sentence):
- Acceptance evidence (observable result and relevant commands):
- Explicit exclusions:
- Context version / MANIFEST.json SHA-256 / decision IDs:
- Code commit or snapshot / branch-worktree / pre-existing dirty files:
- Required reads (specific files/sections; relevant images only):
- Contract/schema versions and required upstream task outputs:
- Allowed write paths and shared resources; integration owner:
- Model tier / effort / why; worker count if any:
- Time or usage budget if known; otherwise qualitative scope:
- Candidate assumptions and how they remain isolated:
- Blocker escalation (affected contract/task, not whole-project pause):

Return: outcome, changed paths/diff identity, actual checks/results, evidence paths, remaining risks and next dependency. If scope or interface must change, notify the lead before writing outside ownership. Do not mark evidence run when it is merely planned.
