# S06 — Expansion package slice

**Preferred model:** GPT-5.6 Sol, medium/high
**Write paths:** `src/features/expansion/**` after I01 publishes the action/state contract.

Build one existing account with explicit needs and a small merge/package interaction. The player should trade package fit against service load. Successful fit creates addon ARR only through the published semantic action.

Fail if it is a generic merge game whose objects have no account meaning. Keep the content set tiny.
