# S07 — Operations pressure slice

**Preferred model:** GPT-6 Astra, high
**Write paths:** `src/features/operations/**` plus only the engine adapter paths explicitly assigned by the integration owner.

Build one accumulated operational problem that is inspectable before catastrophe. Scratch/reveal evidence, diagnose a cause, then choose a resolution. Include one explicit high-variance optimizer option whose stake/upside/downside are visible before commitment.

The outcome must change a real Operations state such as incident backlog, strain, rot, reliability or cost. Do not emit generic coins. Fail if the risky option is secretly mandatory, secretly catastrophic or a guaranteed reward.
