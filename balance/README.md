# Balance

This folder contains machine-readable quantitative status and, once genuinely locked, runtime balance.

Read `../docs/balance/BALANCE_SPEC_V2.md` first.

Current V2 state:

- causal/economic relationships: partially locked in product canon;
- many numeric surfaces: unresolved;
- `v2/registry.json`: authoritative status inventory;
- `v2/runtime.json`: intentionally absent until `runtimeReady` becomes true.

Never import `registry.json` into production presentation as if provisional values were final.
