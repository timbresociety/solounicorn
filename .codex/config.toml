# Project-local Codex configuration for ONE PERSON UNICORN.
# Codex only loads project .codex config for trusted repositories.

project_doc_max_bytes = 65536

[mcp_servers.playwright]
command = "npx"
args = ["-y", "@playwright/mcp@latest"]
