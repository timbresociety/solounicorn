# Optional Codex MCP configuration.
# Start with Playwright only. Add Chrome DevTools only when needed.

[mcp_servers.playwright]
command = "npx"
args = ["-y", "@playwright/mcp@latest"]

# Uncomment when you specifically need DevTools-level debugging/performance analysis.
# [mcp_servers.chrome-devtools]
# command = "npx"
# args = ["-y", "chrome-devtools-mcp@latest"]
