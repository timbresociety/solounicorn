# S04 — Cook a Product and serve its customer

Status: see [queue.json](../queue.json), the single status record.

- Model: Terra / medium.
- Depends on: S03.
- Owns: Extracted Product room, recipe content, product action/reservation adapters, focused tests; shared engine changes reviewed.
- Read: [DECISIONS.md](../DECISIONS.md), [CHECKPOINT.md](../CHECKPOINT.md), D03–D06; G06/G08; recipe model and starter Product interaction principles.

## Outcome and small checkpoints

1. Implement prepare/place/verify/serve as a cooking-style station with meaningful ingredient/order affordances. Keep recipe and time visible without verbose prose.
2. Use real acquired jobs, explicit incomplete/correct/incorrect/verified/shipped states and persistent partial assembly. Editing invalidates verification. Agents cannot steal a reserved order.
3. Finish Demand → Product → Monetisation → service → invoice → collection using actual UI controls and result cues. Diagnostic fixtures supplement but do not replace an earned first customer.

## Acceptance

- A player can build the recipe from visible cues; wrong assembly gives a local correction cue before shipping.
- Ingredients, targets and primary actions are usable together on mobile; no drag requiring inaccessible offscreen targets.
- Ship consumes the exact job once, creates a trial once and never directly creates ARR.
- Navigation/reload/expiry/agent contention preserve or explicitly retire work, never silently reset it.

## Validation and handoff

Run focused tests for the changed boundary, then root validation/build at integration. Follow the shared gate in [README.md](../README.md). Browser/computer-use is currently disabled: do not execute browser scripts; record visual acceptance as pending. Use actual command paths from S00, never invented npm scripts. No deployment or publication.

After each checkpoint update CHECKPOINT.md with completed behaviour, changed files, versions, last test and next action. Evidence goes under `artifacts/qa/founder-revamp/S04/` with base/diff identity and scenario. Update queue.json only to the evidenced status.

If interrupted, preserve the coherent partial edit and identify the first unverified transition. Roll back only this packet's changes using its recorded diff if necessary; never reset unrelated work or delete saves. A future model resumes here rather than repeating a full audit.
