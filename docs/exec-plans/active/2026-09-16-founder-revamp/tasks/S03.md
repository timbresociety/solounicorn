# S03 — Play Demand inside the garage laptop HUD

Status: see [queue.json](../queue.json), the single status record.

- Model: Terra / medium.
- Depends on: S02.
- Owns: src/components/founder HUD/environment shell and Demand room, scoped styles, demand resolver only as needed, asset manifest/briefs.
- Read: [DECISIONS.md](../DECISIONS.md), [CHECKPOINT.md](../CHECKPOINT.md), D03, D06, D08–D10; G08/G12; starter taste/visual/interaction sections only.

## Outcome and small checkpoints

1. Build the persistent shabby garage/business-laptop composition: top score; left work navigation; right upgrades/consumables/skills/strain/compute/bills. Open Demand inside the laptop screen with one dominant swipe object.
2. Give profiles readable needs, segment, budget/stake and compatibility signals. Swiping spends/declines finite acquisition work and creates a real Product job under rank-0 deterministic action rules.
3. Persist window/job state. Use concise metric cues and a first-use helper. Compose mobile rails/drawers around a large usable laptop screen; do not shrink the entire desktop desk.

## Acceptance

- Pursue/pass commits once and gives a visible downstream queue change and correct cash delta.
- Unfinished actions survive navigation appropriately; pointer cancellation does not spend acquisition cash.
- Score stays readable and primary gesture reachable on 1440, 390 and 320 widths; right-rail critical bills remain accessible on phones.
- Baseline original 2.5D scene/laptop assets exist or an explicit temporary-art boundary and exact asset brief remains open. Final art is required by S13.

## Validation and handoff

Run focused tests for the changed boundary, then root validation/build at integration. Follow the shared gate in [README.md](../README.md). Browser/computer-use is currently disabled: do not execute browser scripts; record visual acceptance as pending. Use actual command paths from S00, never invented npm scripts. No deployment or publication.

After each checkpoint update CHECKPOINT.md with completed behaviour, changed files, versions, last test and next action. Evidence goes under `artifacts/qa/founder-revamp/S03/` with base/diff identity and scenario. Update queue.json only to the evidenced status.

If interrupted, preserve the coherent partial edit and identify the first unverified transition. Roll back only this packet's changes using its recorded diff if necessary; never reset unrelated work or delete saves. A future model resumes here rather than repeating a full audit.
