# S10 — Run and supervise an automated six-function company

Status: see [queue.json](../queue.json), the single status record.

- Model: Terra / medium.
- Depends on: S09.
- Owns: Cross-function work scheduler/reservations, pressure/compute system, agent telemetry, shared progression integration and tests. Sol reviews shared contract changes.
- Read: [DECISIONS.md](../DECISIONS.md), [CHECKPOINT.md](../CHECKPOINT.md), D04/D12; all per-room action contracts and relevant master pressure model.

## Outcome and small checkpoints

1. Integrate all six 16-rank trees for 96 real ranks. Fully automated rooms perform their actual workflow, with visible agent progress and no special fake-output path.
2. Scale adds real capacity and coordination load; automation adds compute/upkeep and context rot. Model causes, thresholds, slowdown/rework and recovery separately. Deterministic pressure consequences at zero non-ticket Luck must not restore hidden error rolls.
3. Allow founder takeover, agent disable/re-enable, queue backpressure and finite credits. Stable configurations earn calm periods; overextension creates legible problems and recovery choices.

## Acceptance

- A company can operate all six routines automatically while its founder changes policies, uses consumables and handles exceptions.
- Manual/agent contention never duplicates or loses a job; same-tick chains and unbounded idle credits cannot mint output.
- Controlled versus overextended builds show causal differences in compute, strain, rot, customer outcomes and cash.
- Agent count, installed capability and active capacity are distinct truthful HUD quantities.

## Validation and handoff

Run focused tests for the changed boundary, then root validation/build at integration. Follow the shared gate in [README.md](../README.md). Browser/computer-use is currently disabled: do not execute browser scripts; record visual acceptance as pending. Use actual command paths from S00, never invented npm scripts. No deployment or publication.

After each checkpoint update CHECKPOINT.md with completed behaviour, changed files, versions, last test and next action. Evidence goes under `artifacts/qa/founder-revamp/S10/` with base/diff identity and scenario. Update queue.json only to the evidenced status.

If interrupted, preserve the coherent partial edit and identify the first unverified transition. Roll back only this packet's changes using its recorded diff if necessary; never reset unrelated work or delete saves. A future model resumes here rather than repeating a full audit.
