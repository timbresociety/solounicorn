# S14 — Ship a resilient local PWA candidate

Status: see [queue.json](../queue.json), the single status record.

- Model: Terra / medium.
- Depends on: S13.
- Owns: App lifecycle, offline manifest/service worker, save recovery, responsive/a11y fixes and necessary build cleanup; Sol for migrations.
- Read: [DECISIONS.md](../DECISIONS.md), [CHECKPOINT.md](../CHECKPOINT.md), D10; latest save/schema/content versions; current PWA helpers and QA scripts.

## Outcome and small checkpoints

1. Verify desktop, phone, tablet/landscape and installed PWA composition. Resolve focus, keyboard/touch parity, safe areas, long amounts and returning to the correct work context.
2. Cache the complete authored game atomically, including assets/content, and activate updates at safe boundaries. Recover interrupted writes and preserve incompatible saves; no offline catch-up debt.
3. Measure representative late-run performance and bound queues/history/effects without dropping economic work. Remove proven obsolete shipping imports and repair required build checks. Preserve historical user data and source packs.

## Acceptance

- Initial online load followed by offline play/reload/quarter break works with the complete art/content catalogue.
- Update during partial work/draft cannot silently change rules or duplicate rewards. Quota/storage failure has an explicit recovery path.
- Required test/typecheck/lint/build pass; responsive and physical-device evidence is named, not inferred.
- Frame-time percentiles and input response under declared device/load are recorded; no frame-rate-dependent economy.

## Validation and handoff

Run focused tests for the changed boundary, then root validation/build at integration. Follow the shared gate in [README.md](../README.md). Browser/computer-use is currently disabled: do not execute browser scripts; record visual acceptance as pending. Use actual command paths from S00, never invented npm scripts. No deployment or publication.

After each checkpoint update CHECKPOINT.md with completed behaviour, changed files, versions, last test and next action. Evidence goes under `artifacts/qa/founder-revamp/S14/` with base/diff identity and scenario. Update queue.json only to the evidenced status.

If interrupted, preserve the coherent partial edit and identify the first unverified transition. Roll back only this packet's changes using its recorded diff if necessary; never reset unrelated work or delete saves. A future model resumes here rather than repeating a full audit.
