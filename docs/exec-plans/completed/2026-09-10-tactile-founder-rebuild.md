# Tactile founder rebuild

Owner instruction, 2026-09-10, overrides prior diagnostic-only Operations and radar Retention presentation. Active authority: master context + docs/FOUNDER_RUNTIME.md. Preserve existing dirty work.

Scope: deterministic scratch reward cards with optional negative cash effects and discard; stable Demand pointer lifecycle; varied Product recipes and verification; spatial Expansion merge tiers and customer fulfillment; Retention breakable banks; smooth pricing timing; automatic quarter-end draft presentation; earned dashboard detail; coherent authored 3D assets.

Engine owns card identities/rewards/settlement, recipe verification, merge board, retention hits, timing score and quarter draft. Pointer positions, scratch masks, interpolation and particles are presentation only. Version new state with migration from candidate.2. Candidate economics remain provisional. No publication.

Validation: semantic/replay/accounting tests, real pointer browser flows at 1440/390/320 widths, cancellation/stale identity, saved progress, reduced motion, validate and build. Report actual evidence and outstanding acceptance/balance boundaries.

## Implemented and verified

Implemented candidate.3 in the founder runtime and `ToyRooms.tsx` / `toys.css`. Added nine recipe briefs, persistent merge boards, scratch-card accounting and replay, bank hits, visible timing input, automatic quarter draft, enhanced capability HUD, two original 3D assets and offline caching. Candidate.2 saves migrate with a preserved replay baseline.

Validation passed: repository contract, balance registry, simulation smoke/determinism, 26 founder tests (13 company, 12 tactile, one six-policy-run test), TypeScript, ESLint, production build. Six candidate policy runs across seeds 19/77/84022 and bootstrap/debt yielded four wins and two explained liquidity failures. This does not establish human balance or target session duration.

Browser evidence: `artifacts/qa/tactile/report.json`, `touch-report.json` and screenshots. Mouse gestures at 1440px; 390px and 320px layouts and flows; native touch swipe, scratch and merge; 320x568 native touch recipe assembly including edge scrolling; keyboard merge after touch; incorrect recipe tests; live offer commit; final bank care; scratch inspect/claim/discard and restored inspection; automatic Q1 draft; reduced motion; no browser exceptions. During QA fixed scratch brush alpha, scroll targeting under sticky HUD, and touch-drag suppression swallowing the next keyboard selection.

Remaining acceptance boundaries: owner interaction/art review; long-run human pacing and balance. No publish/push/deploy performed.

Final progression edge: late-quarter drafts now draw from remaining next-rank capabilities when the strategy pool is depleted, using existing skill costs and installation rules. A quarter can be passed without spending. Verified offline production reload, paused write stability, and corrupt-checkpoint preservation.

Completed local handoff: final production build served on localhost:3101 (original save origin), with localhost:3110 retained as the verification instance. Final production mouse/mobile and native-touch suites pass with zero browser exceptions.
