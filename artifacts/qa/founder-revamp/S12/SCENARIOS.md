# S12 headless acceptance

## Covered

- Twelve authored rewards across four candidate families; ten registered effects, with explicit target, trigger, units, duration, charge rule, limitation and localization context.
- Seeded relevance/eligibility, at most three distinct-effect choices, no owned/full/irrelevant offers, persisted selection once, no second cash charge, exhausted-pool skip and frozen Continue.
- Each relic has a paired diagnostic comparison: actual overhead, future invoice dates, acquisition stake, shipped fit, resolved care deadline, addon-only service cost, compute cost and routine rot. No extra ARR from Product, Retention, Operations, funding or reward selection.
- Operations ticket allocation and the preview use the compute relic consistently with actual recurring bills; ticket RNG and patch value do not change.
- Timed consumables preserve charges and timers, freeze between quarters, expire on the active clock and stop discounting future forecast bills. Stale targets, duplicates, active equivalent effects and depleted items do not consume charges.
- Candidate.11/content.5 saves retain offered choices, relics, charges and financing. Historical over-cap charges survive; invalid negative inventory is rejected. Migration establishes a new replay baseline.
- Real engine unicorn transitions recorded once through reload, storage retry and continuation; quarter-boundary wins retain their draft; failed runs mint no power; legacy history deduplicates without a hidden advantage cap.

## Limits

These are synthetic seeded fixtures and a fixed headless policy, not human pacing, UI interaction or enjoyment evidence. Effect magnitudes remain candidate. All 41 historical required balance entries remain unresolved. Browser/computer-use stays disabled; responsive, gesture, rendered feedback and human acceptance are pending.

## Diagnostic corrections

- The original empty-inventory assertion omitted the newly persisted active-effect list.
- An old toy test required a firebreak in every draft despite having no customers. It now checks relevant bounded choices and free selection.
- The first forecast edit retained an obsolete local cost reference; fixed all bill selectors and reran focused coverage.
- The initial compute-upkeep measurement edit omitted its optional factor parameter; corrected it and reran ticket/content coverage.
- Preliminary full-suite runs were interrupted after finding the old draft assertion and the Operations upkeep mismatch. Their logs are retained separately; the final validation result supersedes them.
- React Compiler rejected a locally derived manual-memo dependency. The Operations preview now uses a small hook with scalar arguments; typecheck/lint and focused tests pass without disabling compiler checks or recomputing the distribution every clock tick.
